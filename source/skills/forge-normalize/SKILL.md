---
name: forge-normalize
description: Align every value in the codebase to the nearest system token. The nuclear option for system compliance.
user-invokable: true
---

# /forge-normalize — Full Normalization

Replace EVERY design value with the nearest system token:

1. Every color → var(--token)
2. Every font-size → var(--type-step)
3. Every spacing → var(--space-n)
4. Every shadow → var(--shadow-level)
5. Every radius → var(--radius-size)

This is aggressive. Shows full before/after diff. Requires explicit confirmation.
